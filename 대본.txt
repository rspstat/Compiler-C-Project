안녕하십니까.
이번 컴파일러 프로젝트 시연에 앞서 제가 만든 프로젝트 언어가 어떻게 동작하는지에 대해 C언어와 비교하여 설명하겠습니다.
우선 사칙연산과, 증감,가감 연산자와 할당 연산자들은 처음에는 제 프로젝트에 맞춰 제작을 하려다가 가독성이 너무 떨어지는 것 같아
이것들은 C언어와 동일하게 사용해주었습니다. 

다음으로 if문입니다. if문은 만약에 ~~가 ~~보다 크다면 요기부터 요기까지 실행할랭과 같이
우리에게 친숙한 어순과 어휘들로 코딩할 수 있도록 하였습니다.

다음은 if-else 문 입니다.
If문과 대개적인 언어는 똑같지만 Else 문을 아니라면으로 하여 더 쉽게 언어를 작성할 수 있도록 하였습니다.
그리고 변수에 값을 저장하는 코드들은 한국어로 ~~에 ~~를 저장할랭과 같이 친숙하게 제작하였고,
C언어에서 마침표 역할인 세미콜론을 이 언어에서는 제거하여 가독성을 더욱 좋게 만들었습니다.

다음은 for문 입니다.
for문 같은 경우에는 고민을 정말 많이 했는데, 일단은 ~~에 ~~를 저장할랭 ~~가 ~~보다 작다면 마지막에 ~~ 하면서 요기부터 요기까지 반복할랭
으로 작성하면 됩니다.

다음은 while문 입니다.
while문 같은 경우에는 한국인이 while문을 읽을 때 생각나는 문장들로 구성을 해보았는데,
~~가 ~~보다 작다면 요기부터 요기까지 반복할랭과 같이 최대한 간결하고 이해하기 쉽도록 제작해보았습니다.

다음은 swap 문 입니다.
C언어에서는 변수 3개를 가지고 최소 3줄 이상의 코드를 작성해야하는 것과는 달리
그냥 ~~와 ~~를 서로 바꿔줭. 과 같이 임시 변수 생성 로직을 활용하여 단 한 줄로 서로 값을 바꿀 수 있도록 하였습니다.

다음은 삼항 연산자입니다.
1학년때 C언어를 처음 배울 때 삼항연산자를 헷갈렸던 경험이 있었습니다. 이에 맞춰 초보자도 쉽게 사용할 수 있도록
c에 x가 y보다 크다면 x를 저장할랭 아니라면 y를 저장할랭과 같이 C언어에 능숙하지 않아도 이해하기 쉽도록 구성하였습니다.

다음은 printf문과 scanf문입니다.
각각 출력과 입력을 담당하는데 여기서 ~~의 값을 출력할랭, ~~의 값을 입력받아줭과 같이 쉬운 구성으로
입출력을 할 수 있도록 하였습니다.

다음은 함수 선언과 호출입니다.
함수를 선언하는 것은 "함수 이름은 ~~이고 뭐가들었냐면 요기부터 요기까지인 함수를 선언할랭으로 마치면서 함수를 선언할 수 있고,
이 함수를 호출할때는 ~~를 호출할랭과 같이 작성하면 함수가 호출됩니다.

마지막으로 switch 문입니다.
스위치문은 여러 경우가 있엉 ~~가로 시작하여
어떤 숫자일 경우로 case문을 작성해주면 됩니다. 
또한 C언어의 break; 대신 빠져나가봥으로 작성하여 해당 동작을 종료시킬 수있습니다.
그리고 default 같은 경우에는 다 아닌경우엔 이라고 작성하면 됩니다.

지금부터 프로젝트 시연하는 모습을 보여드리겠습니다.

